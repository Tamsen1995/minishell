git add -A
git commit -m "[UPD] making the cd move to the home directory.
I think the general logic for the change of directory to the home directory is there. But something is wrong"
git push origin cd/tohome