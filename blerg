git add -A
git commit -m "[UPD] making sure that the args will be split properly even if they are delimited by tabs and not spaces.
Done by a function which will go through the tentative string and then replace potential tabs with spaces"
git push origin fix/tabs