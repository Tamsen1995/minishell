git add -A
git commit -m "[UPD] making the cd move to the home directory.
got the env variables into a list. Now I need to integrate my t_shell structure into the
actual shell"
git push origin cd/tohome