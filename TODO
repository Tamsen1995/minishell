
> There is a pointer that isn't supposed to be freed
    when launching the shell entering /bin/ls and then exit
    FIND IT

> imeplemnt the recognition and execution of binary command paths

> Refactor
> pair every malloc with a free
    go over code again to see if everything was properly freed
    valgrind through the correction