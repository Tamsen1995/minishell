


> Re-do echo command so it takes EVERYTHING after it instead of just the first string

> implement an error message for commands which do not exist

> Refactor
> pair every malloc with a free
    go over code again to see if everything was properly freed
    valgrind through the correction